rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    match /{document=**} {
      // Bloqueamos acceso público. Solo usuarios autenticados pueden operar.
      allow read, write: if request.auth != null;
    }
  }
}
